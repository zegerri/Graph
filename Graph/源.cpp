#include"MyPolyRect.h"
using namespace std;
//MyPolyRect r;
//r.AddRectangle(QPoint(0, 0), QPoint(5, 5)); // 添加一个矩形
//double length = r.compute_length(0); // 计算第一个矩形的周长
//double area = r.compute_Area(0); // 计算第一个矩形的面积